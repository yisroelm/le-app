<h1>Appointments</h1>
<ul>
  <% @appointments.each do |appointment| %>
    <li><%= link_to appointment.client.name, appointment_path(appointment) %></li>
  <% end %>
</ul>
<br>
<%= link_to "Schedule New Appointment", new_appointment_path %>
